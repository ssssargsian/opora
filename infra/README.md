# Infrastructure

Local infrastructure is defined in the root `docker-compose.yml`. Production manifests will be added here only after deployment requirements and secret-management boundaries are agreed.
